// Cоздать одно случайное целое число от 1 до 200
$arr_rand = []; //массив случайных чисел 
$min = 0; // минимальное число
$max = 200; // максимальное число
$count = 200; //диапозон поиска чисел

echo mt_rand($min, $max);
